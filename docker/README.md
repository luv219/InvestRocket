# Docker Placeholder

Container support is intentionally deferred until a later phase.

This directory will eventually contain backend and frontend image definitions plus deployment-oriented configuration. Neon PostgreSQL will remain an external managed database, and no local PostgreSQL container is planned for the primary workflow.
